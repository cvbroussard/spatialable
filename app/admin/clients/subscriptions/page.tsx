import { SubscriptionsClient } from './subscriptions-client';

export const metadata = {
  title: 'Subscriptions — SpatialAble Admin',
};

export default function SubscriptionsPage() {
  return <SubscriptionsClient />;
}
