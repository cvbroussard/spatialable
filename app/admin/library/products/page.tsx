import { ProductsClient } from './products-client';

export const metadata = {
  title: 'Product Sets — SpatialAble Admin',
};

export default function ProductsPage() {
  return <ProductsClient />;
}
