import { MaterialsClient } from './materials-client';

export const metadata = {
  title: 'Materials — SpatialAble Admin',
};

export default function MaterialsPage() {
  return <MaterialsClient />;
}
